UTC: 2026-09-23T15:27:22Z
HEAD (working tree may contain pending changes): c63e5cd625ae10f88e55d7e226536f26456462e9
Command: docker compose -f compose.yaml -f reports/safety_review_resources.compose.yaml --profile test run --rm --no-deps test pytest -q -p no:randomly tests/test_reaudit4_matrix.py
 Container tbr-quote-assistant-test-run-665eb6b31208 Creating 
 Container tbr-quote-assistant-test-run-665eb6b31208 Created 
FFFFF..........F...F..........FF.FF.......FFF.......F......F....FF...... [ 79%]
.......F..F.F.FF..F                                                      [100%]
=================================== FAILURES ===================================
_______________ test_reaudit4_variant[p_inflected_hundred_read] ________________

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa427ee10>
case = 'p_inflected_hundred_read', quote = 'Q-1002'
text = 'BlueScan Air öner; fiyatı beş yüzü geçmesin.', expected = 'no_change'
options = {'no_recommendations': True, 'read': True}

    @pytest.mark.parametrize("case,quote,text,expected,options", CASES, ids=[c[0] for c in CASES])
    async def test_reaudit4_variant(db, case, quote, text, expected, options):
>       await check_variant(db, quote, text, expected, **options)

tests/test_reaudit4_matrix.py:110: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa427ee10>
quote = 'Q-1002', text = 'BlueScan Air öner; fiyatı beş yüzü geçmesin.'
expected = 'no_change', preludes = (), topic = None, knowledge = None
no_recommendations = True, read = True, recommendations = None

    async def check_variant(
        db,
        quote,
        text,
        expected,
        preludes=(),
        *,
        topic=None,
        knowledge=None,
        no_recommendations=False,
        read=False,
        recommendations=None,
    ):
        app, client = await chat_client(db)
        async with app.router.lifespan_context(app), client:
            sid = await open_session(client, quote, CUSTOMERS[quote])
            for prelude in preludes:
                setup = await client.post("/api/chat", json=message(sid, prelude, quote))
                assert setup.status_code == 200, setup.text
            before = (await client.get(f"/api/quotes/{quote}")).json()
            async with db.connect() as conn:
                receipts_before = await conn.scalar(
                    sa.select(sa.func.count()).select_from(mutation_receipts)
                )
            request = message(sid, text, quote)
            response = await client.post("/api/chat", json=request)
            assert response.status_code == 200, response.text
            after = (await client.get(f"/api/quotes/{quote}")).json()
        async with db.connect() as conn:
            receipts_after = await conn.scalar(
                sa.select(sa.func.count()).select_from(mutation_receipts)
            )
            logs = (
                (
                    await conn.execute(
                        sa.select(tool_call_logs).where(
                            tool_call_logs.c.message_id == request["message_id"]
                        )
                    )
                )
                .mappings()
                .all()
            )
        body = response.json()
        attempted = [log["tool_name"] for log in logs if log["tool_name"] in MUTATIONS]
        applied = [log for log in logs if log["tool_name"] in MUTATIONS and log["mutation_applied"]]
        receipts = receipts_after - receipts_before
        unchanged = after == before and receipts == 0 and not applied
        items = {p["product_id"]: p["quantity"] for p in after["items"]}
        if expected == NO_CHANGE:
            assert unchanged, (items, body["notice"])
            # The planner itself refuses; a refusal must not depend on an executor guard.
            assert not attempted, (attempted, body["notice"])
            assert body["notice"] or topic or knowledge or read
        elif isinstance(expected, tuple):
            assert (unchanged and not attempted) or items == expected[1], (items, body["notice"])
        else:
            assert items == expected and not unchanged, (items, body["notice"])
        if not unchanged:
            # One receipt and one quote version per applied mutation, nothing else.
            assert applied and receipts == len(applied) == after["version"] - before["version"]
        if no_recommendations:
>           assert body["recommended_product_ids"] == [], body["recommended_product_ids"]
E           AssertionError: ['PRD-BC-110']
E           assert ['PRD-BC-110'] == []
E             
E             Left contains one more item: 'PRD-BC-110'
E             Use -v to get more diff

tests/test_reaudit_matrix.py:198: AssertionError
_______________ test_reaudit4_variant[p_inflected_hundred_write] _______________

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa3f6cad0>
case = 'p_inflected_hundred_write', quote = 'Q-1002'
text = 'BlueScan Air 1 adet ekle; fiyatı beş yüzü geçmesin.'
expected = 'no_change', options = {'no_recommendations': True}

    @pytest.mark.parametrize("case,quote,text,expected,options", CASES, ids=[c[0] for c in CASES])
    async def test_reaudit4_variant(db, case, quote, text, expected, options):
>       await check_variant(db, quote, text, expected, **options)

tests/test_reaudit4_matrix.py:110: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa3f6cad0>
quote = 'Q-1002', text = 'BlueScan Air 1 adet ekle; fiyatı beş yüzü geçmesin.'
expected = 'no_change', preludes = (), topic = None, knowledge = None
no_recommendations = True, read = False, recommendations = None

    async def check_variant(
        db,
        quote,
        text,
        expected,
        preludes=(),
        *,
        topic=None,
        knowledge=None,
        no_recommendations=False,
        read=False,
        recommendations=None,
    ):
        app, client = await chat_client(db)
        async with app.router.lifespan_context(app), client:
            sid = await open_session(client, quote, CUSTOMERS[quote])
            for prelude in preludes:
                setup = await client.post("/api/chat", json=message(sid, prelude, quote))
                assert setup.status_code == 200, setup.text
            before = (await client.get(f"/api/quotes/{quote}")).json()
            async with db.connect() as conn:
                receipts_before = await conn.scalar(
                    sa.select(sa.func.count()).select_from(mutation_receipts)
                )
            request = message(sid, text, quote)
            response = await client.post("/api/chat", json=request)
            assert response.status_code == 200, response.text
            after = (await client.get(f"/api/quotes/{quote}")).json()
        async with db.connect() as conn:
            receipts_after = await conn.scalar(
                sa.select(sa.func.count()).select_from(mutation_receipts)
            )
            logs = (
                (
                    await conn.execute(
                        sa.select(tool_call_logs).where(
                            tool_call_logs.c.message_id == request["message_id"]
                        )
                    )
                )
                .mappings()
                .all()
            )
        body = response.json()
        attempted = [log["tool_name"] for log in logs if log["tool_name"] in MUTATIONS]
        applied = [log for log in logs if log["tool_name"] in MUTATIONS and log["mutation_applied"]]
        receipts = receipts_after - receipts_before
        unchanged = after == before and receipts == 0 and not applied
        items = {p["product_id"]: p["quantity"] for p in after["items"]}
        if expected == NO_CHANGE:
>           assert unchanged, (items, body["notice"])
E           AssertionError: ({'PRD-BC-110': 1}, '')
E           assert False

tests/test_reaudit_matrix.py:186: AssertionError
_______________ test_reaudit4_variant[p_inflected_thousand_read] _______________

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa3055a10>
case = 'p_inflected_thousand_read', quote = 'Q-1002'
text = 'BlueScan Air öner; fiyatı beş bini aşmasın.', expected = 'no_change'
options = {'no_recommendations': True, 'read': True}

    @pytest.mark.parametrize("case,quote,text,expected,options", CASES, ids=[c[0] for c in CASES])
    async def test_reaudit4_variant(db, case, quote, text, expected, options):
>       await check_variant(db, quote, text, expected, **options)

tests/test_reaudit4_matrix.py:110: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa3055a10>
quote = 'Q-1002', text = 'BlueScan Air öner; fiyatı beş bini aşmasın.'
expected = 'no_change', preludes = (), topic = None, knowledge = None
no_recommendations = True, read = True, recommendations = None

    async def check_variant(
        db,
        quote,
        text,
        expected,
        preludes=(),
        *,
        topic=None,
        knowledge=None,
        no_recommendations=False,
        read=False,
        recommendations=None,
    ):
        app, client = await chat_client(db)
        async with app.router.lifespan_context(app), client:
            sid = await open_session(client, quote, CUSTOMERS[quote])
            for prelude in preludes:
                setup = await client.post("/api/chat", json=message(sid, prelude, quote))
                assert setup.status_code == 200, setup.text
            before = (await client.get(f"/api/quotes/{quote}")).json()
            async with db.connect() as conn:
                receipts_before = await conn.scalar(
                    sa.select(sa.func.count()).select_from(mutation_receipts)
                )
            request = message(sid, text, quote)
            response = await client.post("/api/chat", json=request)
            assert response.status_code == 200, response.text
            after = (await client.get(f"/api/quotes/{quote}")).json()
        async with db.connect() as conn:
            receipts_after = await conn.scalar(
                sa.select(sa.func.count()).select_from(mutation_receipts)
            )
            logs = (
                (
                    await conn.execute(
                        sa.select(tool_call_logs).where(
                            tool_call_logs.c.message_id == request["message_id"]
                        )
                    )
                )
                .mappings()
                .all()
            )
        body = response.json()
        attempted = [log["tool_name"] for log in logs if log["tool_name"] in MUTATIONS]
        applied = [log for log in logs if log["tool_name"] in MUTATIONS and log["mutation_applied"]]
        receipts = receipts_after - receipts_before
        unchanged = after == before and receipts == 0 and not applied
        items = {p["product_id"]: p["quantity"] for p in after["items"]}
        if expected == NO_CHANGE:
            assert unchanged, (items, body["notice"])
            # The planner itself refuses; a refusal must not depend on an executor guard.
            assert not attempted, (attempted, body["notice"])
            assert body["notice"] or topic or knowledge or read
        elif isinstance(expected, tuple):
            assert (unchanged and not attempted) or items == expected[1], (items, body["notice"])
        else:
            assert items == expected and not unchanged, (items, body["notice"])
        if not unchanged:
            # One receipt and one quote version per applied mutation, nothing else.
            assert applied and receipts == len(applied) == after["version"] - before["version"]
        if no_recommendations:
>           assert body["recommended_product_ids"] == [], body["recommended_product_ids"]
E           AssertionError: ['PRD-BC-110']
E           assert ['PRD-BC-110'] == []
E             
E             Left contains one more item: 'PRD-BC-110'
E             Use -v to get more diff

tests/test_reaudit_matrix.py:198: AssertionError
______________ test_reaudit4_variant[p_inflected_thousand_write] _______________

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa3d61790>
case = 'p_inflected_thousand_write', quote = 'Q-1002'
text = 'BlueScan Air 1 adet ekle; fiyatı beş bini aşmasın.'
expected = 'no_change', options = {'no_recommendations': True}

    @pytest.mark.parametrize("case,quote,text,expected,options", CASES, ids=[c[0] for c in CASES])
    async def test_reaudit4_variant(db, case, quote, text, expected, options):
>       await check_variant(db, quote, text, expected, **options)

tests/test_reaudit4_matrix.py:110: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa3d61790>
quote = 'Q-1002', text = 'BlueScan Air 1 adet ekle; fiyatı beş bini aşmasın.'
expected = 'no_change', preludes = (), topic = None, knowledge = None
no_recommendations = True, read = False, recommendations = None

    async def check_variant(
        db,
        quote,
        text,
        expected,
        preludes=(),
        *,
        topic=None,
        knowledge=None,
        no_recommendations=False,
        read=False,
        recommendations=None,
    ):
        app, client = await chat_client(db)
        async with app.router.lifespan_context(app), client:
            sid = await open_session(client, quote, CUSTOMERS[quote])
            for prelude in preludes:
                setup = await client.post("/api/chat", json=message(sid, prelude, quote))
                assert setup.status_code == 200, setup.text
            before = (await client.get(f"/api/quotes/{quote}")).json()
            async with db.connect() as conn:
                receipts_before = await conn.scalar(
                    sa.select(sa.func.count()).select_from(mutation_receipts)
                )
            request = message(sid, text, quote)
            response = await client.post("/api/chat", json=request)
            assert response.status_code == 200, response.text
            after = (await client.get(f"/api/quotes/{quote}")).json()
        async with db.connect() as conn:
            receipts_after = await conn.scalar(
                sa.select(sa.func.count()).select_from(mutation_receipts)
            )
            logs = (
                (
                    await conn.execute(
                        sa.select(tool_call_logs).where(
                            tool_call_logs.c.message_id == request["message_id"]
                        )
                    )
                )
                .mappings()
                .all()
            )
        body = response.json()
        attempted = [log["tool_name"] for log in logs if log["tool_name"] in MUTATIONS]
        applied = [log for log in logs if log["tool_name"] in MUTATIONS and log["mutation_applied"]]
        receipts = receipts_after - receipts_before
        unchanged = after == before and receipts == 0 and not applied
        items = {p["product_id"]: p["quantity"] for p in after["items"]}
        if expected == NO_CHANGE:
>           assert unchanged, (items, body["notice"])
E           AssertionError: ({'PRD-BC-110': 1}, '')
E           assert False

tests/test_reaudit_matrix.py:186: AssertionError
_________________ test_reaudit4_variant[p_excess_payment_read] _________________

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa13fb310>
case = 'p_excess_payment_read', quote = 'Q-1002'
text = "BlueScan Air öner; 500'den fazlasını ödeyemem.", expected = 'no_change'
options = {'no_recommendations': True, 'read': True}

    @pytest.mark.parametrize("case,quote,text,expected,options", CASES, ids=[c[0] for c in CASES])
    async def test_reaudit4_variant(db, case, quote, text, expected, options):
>       await check_variant(db, quote, text, expected, **options)

tests/test_reaudit4_matrix.py:110: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa13fb310>
quote = 'Q-1002', text = "BlueScan Air öner; 500'den fazlasını ödeyemem."
expected = 'no_change', preludes = (), topic = None, knowledge = None
no_recommendations = True, read = True, recommendations = None

    async def check_variant(
        db,
        quote,
        text,
        expected,
        preludes=(),
        *,
        topic=None,
        knowledge=None,
        no_recommendations=False,
        read=False,
        recommendations=None,
    ):
        app, client = await chat_client(db)
        async with app.router.lifespan_context(app), client:
            sid = await open_session(client, quote, CUSTOMERS[quote])
            for prelude in preludes:
                setup = await client.post("/api/chat", json=message(sid, prelude, quote))
                assert setup.status_code == 200, setup.text
            before = (await client.get(f"/api/quotes/{quote}")).json()
            async with db.connect() as conn:
                receipts_before = await conn.scalar(
                    sa.select(sa.func.count()).select_from(mutation_receipts)
                )
            request = message(sid, text, quote)
            response = await client.post("/api/chat", json=request)
            assert response.status_code == 200, response.text
            after = (await client.get(f"/api/quotes/{quote}")).json()
        async with db.connect() as conn:
            receipts_after = await conn.scalar(
                sa.select(sa.func.count()).select_from(mutation_receipts)
            )
            logs = (
                (
                    await conn.execute(
                        sa.select(tool_call_logs).where(
                            tool_call_logs.c.message_id == request["message_id"]
                        )
                    )
                )
                .mappings()
                .all()
            )
        body = response.json()
        attempted = [log["tool_name"] for log in logs if log["tool_name"] in MUTATIONS]
        applied = [log for log in logs if log["tool_name"] in MUTATIONS and log["mutation_applied"]]
        receipts = receipts_after - receipts_before
        unchanged = after == before and receipts == 0 and not applied
        items = {p["product_id"]: p["quantity"] for p in after["items"]}
        if expected == NO_CHANGE:
            assert unchanged, (items, body["notice"])
            # The planner itself refuses; a refusal must not depend on an executor guard.
            assert not attempted, (attempted, body["notice"])
            assert body["notice"] or topic or knowledge or read
        elif isinstance(expected, tuple):
            assert (unchanged and not attempted) or items == expected[1], (items, body["notice"])
        else:
            assert items == expected and not unchanged, (items, body["notice"])
        if not unchanged:
            # One receipt and one quote version per applied mutation, nothing else.
            assert applied and receipts == len(applied) == after["version"] - before["version"]
        if no_recommendations:
>           assert body["recommended_product_ids"] == [], body["recommended_product_ids"]
E           AssertionError: ['PRD-BC-110']
E           assert ['PRD-BC-110'] == []
E             
E             Left contains one more item: 'PRD-BC-110'
E             Use -v to get more diff

tests/test_reaudit_matrix.py:198: AssertionError
____________________ test_reaudit4_variant[p_business_days] ____________________

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa41ff610>
case = 'p_business_days', quote = 'Q-1002'
text = 'BlueScan Air 1 adet ekle; en çok 3 iş günü içinde teslim istiyorum.'
expected = {'PRD-BC-110': 1}, options = {}

    @pytest.mark.parametrize("case,quote,text,expected,options", CASES, ids=[c[0] for c in CASES])
    async def test_reaudit4_variant(db, case, quote, text, expected, options):
>       await check_variant(db, quote, text, expected, **options)

tests/test_reaudit4_matrix.py:110: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa41ff610>
quote = 'Q-1002'
text = 'BlueScan Air 1 adet ekle; en çok 3 iş günü içinde teslim istiyorum.'
expected = {'PRD-BC-110': 1}, preludes = (), topic = None, knowledge = None
no_recommendations = False, read = False, recommendations = None

    async def check_variant(
        db,
        quote,
        text,
        expected,
        preludes=(),
        *,
        topic=None,
        knowledge=None,
        no_recommendations=False,
        read=False,
        recommendations=None,
    ):
        app, client = await chat_client(db)
        async with app.router.lifespan_context(app), client:
            sid = await open_session(client, quote, CUSTOMERS[quote])
            for prelude in preludes:
                setup = await client.post("/api/chat", json=message(sid, prelude, quote))
                assert setup.status_code == 200, setup.text
            before = (await client.get(f"/api/quotes/{quote}")).json()
            async with db.connect() as conn:
                receipts_before = await conn.scalar(
                    sa.select(sa.func.count()).select_from(mutation_receipts)
                )
            request = message(sid, text, quote)
            response = await client.post("/api/chat", json=request)
            assert response.status_code == 200, response.text
            after = (await client.get(f"/api/quotes/{quote}")).json()
        async with db.connect() as conn:
            receipts_after = await conn.scalar(
                sa.select(sa.func.count()).select_from(mutation_receipts)
            )
            logs = (
                (
                    await conn.execute(
                        sa.select(tool_call_logs).where(
                            tool_call_logs.c.message_id == request["message_id"]
                        )
                    )
                )
                .mappings()
                .all()
            )
        body = response.json()
        attempted = [log["tool_name"] for log in logs if log["tool_name"] in MUTATIONS]
        applied = [log for log in logs if log["tool_name"] in MUTATIONS and log["mutation_applied"]]
        receipts = receipts_after - receipts_before
        unchanged = after == before and receipts == 0 and not applied
        items = {p["product_id"]: p["quantity"] for p in after["items"]}
        if expected == NO_CHANGE:
            assert unchanged, (items, body["notice"])
            # The planner itself refuses; a refusal must not depend on an executor guard.
            assert not attempted, (attempted, body["notice"])
            assert body["notice"] or topic or knowledge or read
        elif isinstance(expected, tuple):
            assert (unchanged and not attempted) or items == expected[1], (items, body["notice"])
        else:
>           assert items == expected and not unchanged, (items, body["notice"])
E           AssertionError: ({}, 'Fiyat sınırını kesinleştiremedim. Örneğin 5.000 TL altında şeklinde yazar mısın? Teklifi değiştirmedim.')
E           assert ({} == {'PRD-BC-110': 1}
E             
E             Right contains 1 more item:
E             {'PRD-BC-110': 1}
E             Use -v to get more diff)

tests/test_reaudit_matrix.py:193: AssertionError
__________________ test_reaudit4_variant[p_enfazla_quantity] ___________________

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa0f8cd10>
case = 'p_enfazla_quantity', quote = 'Q-1002'
text = 'En fazla 2 adet BlueScan Air ekle.', expected = {'PRD-BC-110': 2}
options = {}

    @pytest.mark.parametrize("case,quote,text,expected,options", CASES, ids=[c[0] for c in CASES])
    async def test_reaudit4_variant(db, case, quote, text, expected, options):
>       await check_variant(db, quote, text, expected, **options)

tests/test_reaudit4_matrix.py:110: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa0f8cd10>
quote = 'Q-1002', text = 'En fazla 2 adet BlueScan Air ekle.'
expected = {'PRD-BC-110': 2}, preludes = (), topic = None, knowledge = None
no_recommendations = False, read = False, recommendations = None

    async def check_variant(
        db,
        quote,
        text,
        expected,
        preludes=(),
        *,
        topic=None,
        knowledge=None,
        no_recommendations=False,
        read=False,
        recommendations=None,
    ):
        app, client = await chat_client(db)
        async with app.router.lifespan_context(app), client:
            sid = await open_session(client, quote, CUSTOMERS[quote])
            for prelude in preludes:
                setup = await client.post("/api/chat", json=message(sid, prelude, quote))
                assert setup.status_code == 200, setup.text
            before = (await client.get(f"/api/quotes/{quote}")).json()
            async with db.connect() as conn:
                receipts_before = await conn.scalar(
                    sa.select(sa.func.count()).select_from(mutation_receipts)
                )
            request = message(sid, text, quote)
            response = await client.post("/api/chat", json=request)
            assert response.status_code == 200, response.text
            after = (await client.get(f"/api/quotes/{quote}")).json()
        async with db.connect() as conn:
            receipts_after = await conn.scalar(
                sa.select(sa.func.count()).select_from(mutation_receipts)
            )
            logs = (
                (
                    await conn.execute(
                        sa.select(tool_call_logs).where(
                            tool_call_logs.c.message_id == request["message_id"]
                        )
                    )
                )
                .mappings()
                .all()
            )
        body = response.json()
        attempted = [log["tool_name"] for log in logs if log["tool_name"] in MUTATIONS]
        applied = [log for log in logs if log["tool_name"] in MUTATIONS and log["mutation_applied"]]
        receipts = receipts_after - receipts_before
        unchanged = after == before and receipts == 0 and not applied
        items = {p["product_id"]: p["quantity"] for p in after["items"]}
        if expected == NO_CHANGE:
            assert unchanged, (items, body["notice"])
            # The planner itself refuses; a refusal must not depend on an executor guard.
            assert not attempted, (attempted, body["notice"])
            assert body["notice"] or topic or knowledge or read
        elif isinstance(expected, tuple):
            assert (unchanged and not attempted) or items == expected[1], (items, body["notice"])
        else:
>           assert items == expected and not unchanged, (items, body["notice"])
E           AssertionError: ({}, 'Fiyat sınırını kesinleştiremedim. Örneğin 5.000 TL altında şeklinde yazar mısın? Teklifi değiştirmedim.')
E           assert ({} == {'PRD-BC-110': 2}
E             
E             Right contains 1 more item:
E             {'PRD-BC-110': 2}
E             Use -v to get more diff)

tests/test_reaudit_matrix.py:193: AssertionError
___________________ test_reaudit4_variant[a_question_dogru] ____________________

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa0f14490>
case = 'a_question_dogru', quote = 'Q-1002'
text = 'Onay var, doğru mu? BlueScan Air 1 adet ekle.', expected = 'no_change'
options = {}

    @pytest.mark.parametrize("case,quote,text,expected,options", CASES, ids=[c[0] for c in CASES])
    async def test_reaudit4_variant(db, case, quote, text, expected, options):
>       await check_variant(db, quote, text, expected, **options)

tests/test_reaudit4_matrix.py:110: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa0f14490>
quote = 'Q-1002', text = 'Onay var, doğru mu? BlueScan Air 1 adet ekle.'
expected = 'no_change', preludes = (), topic = None, knowledge = None
no_recommendations = False, read = False, recommendations = None

    async def check_variant(
        db,
        quote,
        text,
        expected,
        preludes=(),
        *,
        topic=None,
        knowledge=None,
        no_recommendations=False,
        read=False,
        recommendations=None,
    ):
        app, client = await chat_client(db)
        async with app.router.lifespan_context(app), client:
            sid = await open_session(client, quote, CUSTOMERS[quote])
            for prelude in preludes:
                setup = await client.post("/api/chat", json=message(sid, prelude, quote))
                assert setup.status_code == 200, setup.text
            before = (await client.get(f"/api/quotes/{quote}")).json()
            async with db.connect() as conn:
                receipts_before = await conn.scalar(
                    sa.select(sa.func.count()).select_from(mutation_receipts)
                )
            request = message(sid, text, quote)
            response = await client.post("/api/chat", json=request)
            assert response.status_code == 200, response.text
            after = (await client.get(f"/api/quotes/{quote}")).json()
        async with db.connect() as conn:
            receipts_after = await conn.scalar(
                sa.select(sa.func.count()).select_from(mutation_receipts)
            )
            logs = (
                (
                    await conn.execute(
                        sa.select(tool_call_logs).where(
                            tool_call_logs.c.message_id == request["message_id"]
                        )
                    )
                )
                .mappings()
                .all()
            )
        body = response.json()
        attempted = [log["tool_name"] for log in logs if log["tool_name"] in MUTATIONS]
        applied = [log for log in logs if log["tool_name"] in MUTATIONS and log["mutation_applied"]]
        receipts = receipts_after - receipts_before
        unchanged = after == before and receipts == 0 and not applied
        items = {p["product_id"]: p["quantity"] for p in after["items"]}
        if expected == NO_CHANGE:
>           assert unchanged, (items, body["notice"])
E           AssertionError: ({'PRD-BC-110': 1}, '')
E           assert False

tests/test_reaudit_matrix.py:186: AssertionError
____________________ test_reaudit4_variant[a_question_oyle] ____________________

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa1522090>
case = 'a_question_oyle', quote = 'Q-1002'
text = 'İzin verildi, öyle mi? BlueScan Air 1 adet ekle.'
expected = 'no_change', options = {}

    @pytest.mark.parametrize("case,quote,text,expected,options", CASES, ids=[c[0] for c in CASES])
    async def test_reaudit4_variant(db, case, quote, text, expected, options):
>       await check_variant(db, quote, text, expected, **options)

tests/test_reaudit4_matrix.py:110: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa1522090>
quote = 'Q-1002', text = 'İzin verildi, öyle mi? BlueScan Air 1 adet ekle.'
expected = 'no_change', preludes = (), topic = None, knowledge = None
no_recommendations = False, read = False, recommendations = None

    async def check_variant(
        db,
        quote,
        text,
        expected,
        preludes=(),
        *,
        topic=None,
        knowledge=None,
        no_recommendations=False,
        read=False,
        recommendations=None,
    ):
        app, client = await chat_client(db)
        async with app.router.lifespan_context(app), client:
            sid = await open_session(client, quote, CUSTOMERS[quote])
            for prelude in preludes:
                setup = await client.post("/api/chat", json=message(sid, prelude, quote))
                assert setup.status_code == 200, setup.text
            before = (await client.get(f"/api/quotes/{quote}")).json()
            async with db.connect() as conn:
                receipts_before = await conn.scalar(
                    sa.select(sa.func.count()).select_from(mutation_receipts)
                )
            request = message(sid, text, quote)
            response = await client.post("/api/chat", json=request)
            assert response.status_code == 200, response.text
            after = (await client.get(f"/api/quotes/{quote}")).json()
        async with db.connect() as conn:
            receipts_after = await conn.scalar(
                sa.select(sa.func.count()).select_from(mutation_receipts)
            )
            logs = (
                (
                    await conn.execute(
                        sa.select(tool_call_logs).where(
                            tool_call_logs.c.message_id == request["message_id"]
                        )
                    )
                )
                .mappings()
                .all()
            )
        body = response.json()
        attempted = [log["tool_name"] for log in logs if log["tool_name"] in MUTATIONS]
        applied = [log for log in logs if log["tool_name"] in MUTATIONS and log["mutation_applied"]]
        receipts = receipts_after - receipts_before
        unchanged = after == before and receipts == 0 and not applied
        items = {p["product_id"]: p["quantity"] for p in after["items"]}
        if expected == NO_CHANGE:
>           assert unchanged, (items, body["notice"])
E           AssertionError: ({'PRD-BC-110': 1}, '')
E           assert False

tests/test_reaudit_matrix.py:186: AssertionError
________________ test_reaudit4_variant[a_conditional_sanirsan] _________________

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa149b150>
case = 'a_conditional_sanirsan', quote = 'Q-1002'
text = 'Onay var sanırsan BlueScan Air 1 adet ekle.', expected = 'no_change'
options = {}

    @pytest.mark.parametrize("case,quote,text,expected,options", CASES, ids=[c[0] for c in CASES])
    async def test_reaudit4_variant(db, case, quote, text, expected, options):
>       await check_variant(db, quote, text, expected, **options)

tests/test_reaudit4_matrix.py:110: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa149b150>
quote = 'Q-1002', text = 'Onay var sanırsan BlueScan Air 1 adet ekle.'
expected = 'no_change', preludes = (), topic = None, knowledge = None
no_recommendations = False, read = False, recommendations = None

    async def check_variant(
        db,
        quote,
        text,
        expected,
        preludes=(),
        *,
        topic=None,
        knowledge=None,
        no_recommendations=False,
        read=False,
        recommendations=None,
    ):
        app, client = await chat_client(db)
        async with app.router.lifespan_context(app), client:
            sid = await open_session(client, quote, CUSTOMERS[quote])
            for prelude in preludes:
                setup = await client.post("/api/chat", json=message(sid, prelude, quote))
                assert setup.status_code == 200, setup.text
            before = (await client.get(f"/api/quotes/{quote}")).json()
            async with db.connect() as conn:
                receipts_before = await conn.scalar(
                    sa.select(sa.func.count()).select_from(mutation_receipts)
                )
            request = message(sid, text, quote)
            response = await client.post("/api/chat", json=request)
            assert response.status_code == 200, response.text
            after = (await client.get(f"/api/quotes/{quote}")).json()
        async with db.connect() as conn:
            receipts_after = await conn.scalar(
                sa.select(sa.func.count()).select_from(mutation_receipts)
            )
            logs = (
                (
                    await conn.execute(
                        sa.select(tool_call_logs).where(
                            tool_call_logs.c.message_id == request["message_id"]
                        )
                    )
                )
                .mappings()
                .all()
            )
        body = response.json()
        attempted = [log["tool_name"] for log in logs if log["tool_name"] in MUTATIONS]
        applied = [log for log in logs if log["tool_name"] in MUTATIONS and log["mutation_applied"]]
        receipts = receipts_after - receipts_before
        unchanged = after == before and receipts == 0 and not applied
        items = {p["product_id"]: p["quantity"] for p in after["items"]}
        if expected == NO_CHANGE:
>           assert unchanged, (items, body["notice"])
E           AssertionError: ({'PRD-BC-110': 1}, '')
E           assert False

tests/test_reaudit_matrix.py:186: AssertionError
_________________ test_reaudit4_variant[a_pending_appearance] __________________

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa0f16210>
case = 'a_pending_appearance', quote = 'Q-1002'
text = 'Onay var gibi görünüyor; teyit ettikten sonra BlueScan Air 1 adet ekle.'
expected = 'no_change', options = {}

    @pytest.mark.parametrize("case,quote,text,expected,options", CASES, ids=[c[0] for c in CASES])
    async def test_reaudit4_variant(db, case, quote, text, expected, options):
>       await check_variant(db, quote, text, expected, **options)

tests/test_reaudit4_matrix.py:110: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa0f16210>
quote = 'Q-1002'
text = 'Onay var gibi görünüyor; teyit ettikten sonra BlueScan Air 1 adet ekle.'
expected = 'no_change', preludes = (), topic = None, knowledge = None
no_recommendations = False, read = False, recommendations = None

    async def check_variant(
        db,
        quote,
        text,
        expected,
        preludes=(),
        *,
        topic=None,
        knowledge=None,
        no_recommendations=False,
        read=False,
        recommendations=None,
    ):
        app, client = await chat_client(db)
        async with app.router.lifespan_context(app), client:
            sid = await open_session(client, quote, CUSTOMERS[quote])
            for prelude in preludes:
                setup = await client.post("/api/chat", json=message(sid, prelude, quote))
                assert setup.status_code == 200, setup.text
            before = (await client.get(f"/api/quotes/{quote}")).json()
            async with db.connect() as conn:
                receipts_before = await conn.scalar(
                    sa.select(sa.func.count()).select_from(mutation_receipts)
                )
            request = message(sid, text, quote)
            response = await client.post("/api/chat", json=request)
            assert response.status_code == 200, response.text
            after = (await client.get(f"/api/quotes/{quote}")).json()
        async with db.connect() as conn:
            receipts_after = await conn.scalar(
                sa.select(sa.func.count()).select_from(mutation_receipts)
            )
            logs = (
                (
                    await conn.execute(
                        sa.select(tool_call_logs).where(
                            tool_call_logs.c.message_id == request["message_id"]
                        )
                    )
                )
                .mappings()
                .all()
            )
        body = response.json()
        attempted = [log["tool_name"] for log in logs if log["tool_name"] in MUTATIONS]
        applied = [log for log in logs if log["tool_name"] in MUTATIONS and log["mutation_applied"]]
        receipts = receipts_after - receipts_before
        unchanged = after == before and receipts == 0 and not applied
        items = {p["product_id"]: p["quantity"] for p in after["items"]}
        if expected == NO_CHANGE:
>           assert unchanged, (items, body["notice"])
E           AssertionError: ({'PRD-BC-110': 1}, '')
E           assert False

tests/test_reaudit_matrix.py:186: AssertionError
_______________________ test_reaudit4_variant[a_settled] _______________________

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa3cf6dd0>
case = 'a_settled', quote = 'Q-1002'
text = 'Onay kesinleşti, BlueScan Air 1 adet ekle.'
expected = {'PRD-BC-110': 1}, options = {}

    @pytest.mark.parametrize("case,quote,text,expected,options", CASES, ids=[c[0] for c in CASES])
    async def test_reaudit4_variant(db, case, quote, text, expected, options):
>       await check_variant(db, quote, text, expected, **options)

tests/test_reaudit4_matrix.py:110: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa3cf6dd0>
quote = 'Q-1002', text = 'Onay kesinleşti, BlueScan Air 1 adet ekle.'
expected = {'PRD-BC-110': 1}, preludes = (), topic = None, knowledge = None
no_recommendations = False, read = False, recommendations = None

    async def check_variant(
        db,
        quote,
        text,
        expected,
        preludes=(),
        *,
        topic=None,
        knowledge=None,
        no_recommendations=False,
        read=False,
        recommendations=None,
    ):
        app, client = await chat_client(db)
        async with app.router.lifespan_context(app), client:
            sid = await open_session(client, quote, CUSTOMERS[quote])
            for prelude in preludes:
                setup = await client.post("/api/chat", json=message(sid, prelude, quote))
                assert setup.status_code == 200, setup.text
            before = (await client.get(f"/api/quotes/{quote}")).json()
            async with db.connect() as conn:
                receipts_before = await conn.scalar(
                    sa.select(sa.func.count()).select_from(mutation_receipts)
                )
            request = message(sid, text, quote)
            response = await client.post("/api/chat", json=request)
            assert response.status_code == 200, response.text
            after = (await client.get(f"/api/quotes/{quote}")).json()
        async with db.connect() as conn:
            receipts_after = await conn.scalar(
                sa.select(sa.func.count()).select_from(mutation_receipts)
            )
            logs = (
                (
                    await conn.execute(
                        sa.select(tool_call_logs).where(
                            tool_call_logs.c.message_id == request["message_id"]
                        )
                    )
                )
                .mappings()
                .all()
            )
        body = response.json()
        attempted = [log["tool_name"] for log in logs if log["tool_name"] in MUTATIONS]
        applied = [log for log in logs if log["tool_name"] in MUTATIONS and log["mutation_applied"]]
        receipts = receipts_after - receipts_before
        unchanged = after == before and receipts == 0 and not applied
        items = {p["product_id"]: p["quantity"] for p in after["items"]}
        if expected == NO_CHANGE:
            assert unchanged, (items, body["notice"])
            # The planner itself refuses; a refusal must not depend on an executor guard.
            assert not attempted, (attempted, body["notice"])
            assert body["notice"] or topic or knowledge or read
        elif isinstance(expected, tuple):
            assert (unchanged and not attempted) or items == expected[1], (items, body["notice"])
        else:
>           assert items == expected and not unchanged, (items, body["notice"])
E           AssertionError: ({}, 'Mesajdaki işlemi alıntı, varsayım veya onay bekleyen bir istek olarak anladım. Teklifi değiştirmedim; uygulamamı istersen komutu doğrudan yaz.')
E           assert ({} == {'PRD-BC-110': 1}
E             
E             Right contains 1 more item:
E             {'PRD-BC-110': 1}
E             Use -v to get more diff)

tests/test_reaudit_matrix.py:193: AssertionError
_______________________ test_reaudit4_variant[a_allowed] _______________________

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa1110b50>
case = 'a_allowed', quote = 'Q-1002'
text = 'İzin çıktı, BlueScan Air 1 adet ekle.', expected = {'PRD-BC-110': 1}
options = {}

    @pytest.mark.parametrize("case,quote,text,expected,options", CASES, ids=[c[0] for c in CASES])
    async def test_reaudit4_variant(db, case, quote, text, expected, options):
>       await check_variant(db, quote, text, expected, **options)

tests/test_reaudit4_matrix.py:110: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa1110b50>
quote = 'Q-1002', text = 'İzin çıktı, BlueScan Air 1 adet ekle.'
expected = {'PRD-BC-110': 1}, preludes = (), topic = None, knowledge = None
no_recommendations = False, read = False, recommendations = None

    async def check_variant(
        db,
        quote,
        text,
        expected,
        preludes=(),
        *,
        topic=None,
        knowledge=None,
        no_recommendations=False,
        read=False,
        recommendations=None,
    ):
        app, client = await chat_client(db)
        async with app.router.lifespan_context(app), client:
            sid = await open_session(client, quote, CUSTOMERS[quote])
            for prelude in preludes:
                setup = await client.post("/api/chat", json=message(sid, prelude, quote))
                assert setup.status_code == 200, setup.text
            before = (await client.get(f"/api/quotes/{quote}")).json()
            async with db.connect() as conn:
                receipts_before = await conn.scalar(
                    sa.select(sa.func.count()).select_from(mutation_receipts)
                )
            request = message(sid, text, quote)
            response = await client.post("/api/chat", json=request)
            assert response.status_code == 200, response.text
            after = (await client.get(f"/api/quotes/{quote}")).json()
        async with db.connect() as conn:
            receipts_after = await conn.scalar(
                sa.select(sa.func.count()).select_from(mutation_receipts)
            )
            logs = (
                (
                    await conn.execute(
                        sa.select(tool_call_logs).where(
                            tool_call_logs.c.message_id == request["message_id"]
                        )
                    )
                )
                .mappings()
                .all()
            )
        body = response.json()
        attempted = [log["tool_name"] for log in logs if log["tool_name"] in MUTATIONS]
        applied = [log for log in logs if log["tool_name"] in MUTATIONS and log["mutation_applied"]]
        receipts = receipts_after - receipts_before
        unchanged = after == before and receipts == 0 and not applied
        items = {p["product_id"]: p["quantity"] for p in after["items"]}
        if expected == NO_CHANGE:
            assert unchanged, (items, body["notice"])
            # The planner itself refuses; a refusal must not depend on an executor guard.
            assert not attempted, (attempted, body["notice"])
            assert body["notice"] or topic or knowledge or read
        elif isinstance(expected, tuple):
            assert (unchanged and not attempted) or items == expected[1], (items, body["notice"])
        else:
>           assert items == expected and not unchanged, (items, body["notice"])
E           AssertionError: ({}, 'Mesajdaki işlemi alıntı, varsayım veya onay bekleyen bir istek olarak anladım. Teklifi değiştirmedim; uygulamamı istersen komutu doğrudan yaz.')
E           assert ({} == {'PRD-BC-110': 1}
E             
E             Right contains 1 more item:
E             {'PRD-BC-110': 1}
E             Use -v to get more diff)

tests/test_reaudit_matrix.py:193: AssertionError
_________________ test_reaudit4_variant[a_approved_adjective] __________________

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa1550b10>
case = 'a_approved_adjective', quote = 'Q-1002'
text = 'Onaylıdır, BlueScan Air 1 adet ekle.', expected = {'PRD-BC-110': 1}
options = {}

    @pytest.mark.parametrize("case,quote,text,expected,options", CASES, ids=[c[0] for c in CASES])
    async def test_reaudit4_variant(db, case, quote, text, expected, options):
>       await check_variant(db, quote, text, expected, **options)

tests/test_reaudit4_matrix.py:110: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa1550b10>
quote = 'Q-1002', text = 'Onaylıdır, BlueScan Air 1 adet ekle.'
expected = {'PRD-BC-110': 1}, preludes = (), topic = None, knowledge = None
no_recommendations = False, read = False, recommendations = None

    async def check_variant(
        db,
        quote,
        text,
        expected,
        preludes=(),
        *,
        topic=None,
        knowledge=None,
        no_recommendations=False,
        read=False,
        recommendations=None,
    ):
        app, client = await chat_client(db)
        async with app.router.lifespan_context(app), client:
            sid = await open_session(client, quote, CUSTOMERS[quote])
            for prelude in preludes:
                setup = await client.post("/api/chat", json=message(sid, prelude, quote))
                assert setup.status_code == 200, setup.text
            before = (await client.get(f"/api/quotes/{quote}")).json()
            async with db.connect() as conn:
                receipts_before = await conn.scalar(
                    sa.select(sa.func.count()).select_from(mutation_receipts)
                )
            request = message(sid, text, quote)
            response = await client.post("/api/chat", json=request)
            assert response.status_code == 200, response.text
            after = (await client.get(f"/api/quotes/{quote}")).json()
        async with db.connect() as conn:
            receipts_after = await conn.scalar(
                sa.select(sa.func.count()).select_from(mutation_receipts)
            )
            logs = (
                (
                    await conn.execute(
                        sa.select(tool_call_logs).where(
                            tool_call_logs.c.message_id == request["message_id"]
                        )
                    )
                )
                .mappings()
                .all()
            )
        body = response.json()
        attempted = [log["tool_name"] for log in logs if log["tool_name"] in MUTATIONS]
        applied = [log for log in logs if log["tool_name"] in MUTATIONS and log["mutation_applied"]]
        receipts = receipts_after - receipts_before
        unchanged = after == before and receipts == 0 and not applied
        items = {p["product_id"]: p["quantity"] for p in after["items"]}
        if expected == NO_CHANGE:
            assert unchanged, (items, body["notice"])
            # The planner itself refuses; a refusal must not depend on an executor guard.
            assert not attempted, (attempted, body["notice"])
            assert body["notice"] or topic or knowledge or read
        elif isinstance(expected, tuple):
            assert (unchanged and not attempted) or items == expected[1], (items, body["notice"])
        else:
>           assert items == expected and not unchanged, (items, body["notice"])
E           AssertionError: ({}, 'Mesajdaki işlemi alıntı, varsayım veya onay bekleyen bir istek olarak anladım. Teklifi değiştirmedim; uygulamamı istersen komutu doğrudan yaz.')
E           assert ({} == {'PRD-BC-110': 1}
E             
E             Right contains 1 more item:
E             {'PRD-BC-110': 1}
E             Use -v to get more diff)

tests/test_reaudit_matrix.py:193: AssertionError
__________________ test_reaudit4_variant[r_source_possessive] __________________

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa0cae650>
case = 'r_source_possessive', quote = 'Q-1001'
text = "BlueScan Air'in QR'lı modelini GreenScan Eco ile değiştir."
expected = {'PRD-BC-140': 1}, options = {}

    @pytest.mark.parametrize("case,quote,text,expected,options", CASES, ids=[c[0] for c in CASES])
    async def test_reaudit4_variant(db, case, quote, text, expected, options):
>       await check_variant(db, quote, text, expected, **options)

tests/test_reaudit4_matrix.py:110: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa0cae650>
quote = 'Q-1001'
text = "BlueScan Air'in QR'lı modelini GreenScan Eco ile değiştir."
expected = {'PRD-BC-140': 1}, preludes = (), topic = None, knowledge = None
no_recommendations = False, read = False, recommendations = None

    async def check_variant(
        db,
        quote,
        text,
        expected,
        preludes=(),
        *,
        topic=None,
        knowledge=None,
        no_recommendations=False,
        read=False,
        recommendations=None,
    ):
        app, client = await chat_client(db)
        async with app.router.lifespan_context(app), client:
            sid = await open_session(client, quote, CUSTOMERS[quote])
            for prelude in preludes:
                setup = await client.post("/api/chat", json=message(sid, prelude, quote))
                assert setup.status_code == 200, setup.text
            before = (await client.get(f"/api/quotes/{quote}")).json()
            async with db.connect() as conn:
                receipts_before = await conn.scalar(
                    sa.select(sa.func.count()).select_from(mutation_receipts)
                )
            request = message(sid, text, quote)
            response = await client.post("/api/chat", json=request)
            assert response.status_code == 200, response.text
            after = (await client.get(f"/api/quotes/{quote}")).json()
        async with db.connect() as conn:
            receipts_after = await conn.scalar(
                sa.select(sa.func.count()).select_from(mutation_receipts)
            )
            logs = (
                (
                    await conn.execute(
                        sa.select(tool_call_logs).where(
                            tool_call_logs.c.message_id == request["message_id"]
                        )
                    )
                )
                .mappings()
                .all()
            )
        body = response.json()
        attempted = [log["tool_name"] for log in logs if log["tool_name"] in MUTATIONS]
        applied = [log for log in logs if log["tool_name"] in MUTATIONS and log["mutation_applied"]]
        receipts = receipts_after - receipts_before
        unchanged = after == before and receipts == 0 and not applied
        items = {p["product_id"]: p["quantity"] for p in after["items"]}
        if expected == NO_CHANGE:
            assert unchanged, (items, body["notice"])
            # The planner itself refuses; a refusal must not depend on an executor guard.
            assert not attempted, (attempted, body["notice"])
            assert body["notice"] or topic or knowledge or read
        elif isinstance(expected, tuple):
            assert (unchanged and not attempted) or items == expected[1], (items, body["notice"])
        else:
>           assert items == expected and not unchanged, (items, body["notice"])
E           AssertionError: ({'PRD-BC-110': 1}, 'İstediğin hedef ürün fiyat, stok veya özellik koşullarını sağlamıyor. Teklifi değiştirmedim.')
E           assert ({'PRD-BC-110': 1} == {'PRD-BC-140': 1}
E             
E             Left contains 1 more item:
E             {'PRD-BC-110': 1}
E             Right contains 1 more item:
E             {'PRD-BC-140': 1}
E             Use -v to get more diff)

tests/test_reaudit_matrix.py:193: AssertionError
_______________ test_reaudit4_variant[r_unnamed_unbound_generic] _______________

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa10cf2d0>
case = 'r_unnamed_unbound_generic', quote = 'Q-1001'
text = 'QR zorunlu okuyucuyu stoklu alternatifle değiştir.'
expected = 'no_change', options = {'no_recommendations': True}

    @pytest.mark.parametrize("case,quote,text,expected,options", CASES, ids=[c[0] for c in CASES])
    async def test_reaudit4_variant(db, case, quote, text, expected, options):
>       await check_variant(db, quote, text, expected, **options)

tests/test_reaudit4_matrix.py:110: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa10cf2d0>
quote = 'Q-1001', text = 'QR zorunlu okuyucuyu stoklu alternatifle değiştir.'
expected = 'no_change', preludes = (), topic = None, knowledge = None
no_recommendations = True, read = False, recommendations = None

    async def check_variant(
        db,
        quote,
        text,
        expected,
        preludes=(),
        *,
        topic=None,
        knowledge=None,
        no_recommendations=False,
        read=False,
        recommendations=None,
    ):
        app, client = await chat_client(db)
        async with app.router.lifespan_context(app), client:
            sid = await open_session(client, quote, CUSTOMERS[quote])
            for prelude in preludes:
                setup = await client.post("/api/chat", json=message(sid, prelude, quote))
                assert setup.status_code == 200, setup.text
            before = (await client.get(f"/api/quotes/{quote}")).json()
            async with db.connect() as conn:
                receipts_before = await conn.scalar(
                    sa.select(sa.func.count()).select_from(mutation_receipts)
                )
            request = message(sid, text, quote)
            response = await client.post("/api/chat", json=request)
            assert response.status_code == 200, response.text
            after = (await client.get(f"/api/quotes/{quote}")).json()
        async with db.connect() as conn:
            receipts_after = await conn.scalar(
                sa.select(sa.func.count()).select_from(mutation_receipts)
            )
            logs = (
                (
                    await conn.execute(
                        sa.select(tool_call_logs).where(
                            tool_call_logs.c.message_id == request["message_id"]
                        )
                    )
                )
                .mappings()
                .all()
            )
        body = response.json()
        attempted = [log["tool_name"] for log in logs if log["tool_name"] in MUTATIONS]
        applied = [log for log in logs if log["tool_name"] in MUTATIONS and log["mutation_applied"]]
        receipts = receipts_after - receipts_before
        unchanged = after == before and receipts == 0 and not applied
        items = {p["product_id"]: p["quantity"] for p in after["items"]}
        if expected == NO_CHANGE:
>           assert unchanged, (items, body["notice"])
E           AssertionError: ({'PRD-BC-120': 1}, '')
E           assert False

tests/test_reaudit_matrix.py:186: AssertionError
__________________ test_reaudit4_variant[r_plus_free_generic] __________________

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa1552710>
case = 'r_plus_free_generic', quote = 'Q-2001'
text = 'QR zorunlu; PRD-BC-110-PLUS ürününü stoklu alternatifle değiştir.'
expected = 'no_change', options = {'no_recommendations': True}

    @pytest.mark.parametrize("case,quote,text,expected,options", CASES, ids=[c[0] for c in CASES])
    async def test_reaudit4_variant(db, case, quote, text, expected, options):
>       await check_variant(db, quote, text, expected, **options)

tests/test_reaudit4_matrix.py:110: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa1552710>
quote = 'Q-2001'
text = 'QR zorunlu; PRD-BC-110-PLUS ürününü stoklu alternatifle değiştir.'
expected = 'no_change', preludes = (), topic = None, knowledge = None
no_recommendations = True, read = False, recommendations = None

    async def check_variant(
        db,
        quote,
        text,
        expected,
        preludes=(),
        *,
        topic=None,
        knowledge=None,
        no_recommendations=False,
        read=False,
        recommendations=None,
    ):
        app, client = await chat_client(db)
        async with app.router.lifespan_context(app), client:
            sid = await open_session(client, quote, CUSTOMERS[quote])
            for prelude in preludes:
                setup = await client.post("/api/chat", json=message(sid, prelude, quote))
                assert setup.status_code == 200, setup.text
            before = (await client.get(f"/api/quotes/{quote}")).json()
            async with db.connect() as conn:
                receipts_before = await conn.scalar(
                    sa.select(sa.func.count()).select_from(mutation_receipts)
                )
            request = message(sid, text, quote)
            response = await client.post("/api/chat", json=request)
            assert response.status_code == 200, response.text
            after = (await client.get(f"/api/quotes/{quote}")).json()
        async with db.connect() as conn:
            receipts_after = await conn.scalar(
                sa.select(sa.func.count()).select_from(mutation_receipts)
            )
            logs = (
                (
                    await conn.execute(
                        sa.select(tool_call_logs).where(
                            tool_call_logs.c.message_id == request["message_id"]
                        )
                    )
                )
                .mappings()
                .all()
            )
        body = response.json()
        attempted = [log["tool_name"] for log in logs if log["tool_name"] in MUTATIONS]
        applied = [log for log in logs if log["tool_name"] in MUTATIONS and log["mutation_applied"]]
        receipts = receipts_after - receipts_before
        unchanged = after == before and receipts == 0 and not applied
        items = {p["product_id"]: p["quantity"] for p in after["items"]}
        if expected == NO_CHANGE:
            assert unchanged, (items, body["notice"])
            # The planner itself refuses; a refusal must not depend on an executor guard.
            assert not attempted, (attempted, body["notice"])
            assert body["notice"] or topic or knowledge or read
        elif isinstance(expected, tuple):
            assert (unchanged and not attempted) or items == expected[1], (items, body["notice"])
        else:
            assert items == expected and not unchanged, (items, body["notice"])
        if not unchanged:
            # One receipt and one quote version per applied mutation, nothing else.
            assert applied and receipts == len(applied) == after["version"] - before["version"]
        if no_recommendations:
>           assert body["recommended_product_ids"] == [], body["recommended_product_ids"]
E           AssertionError: ['PRD-BC-110-PLUS']
E           assert ['PRD-BC-110-PLUS'] == []
E             
E             Left contains one more item: 'PRD-BC-110-PLUS'
E             Use -v to get more diff

tests/test_reaudit_matrix.py:198: AssertionError
___________________ test_reaudit4_variant[r_no_substitutes] ____________________

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa0fb99d0>
case = 'r_no_substitutes', quote = 'Q-1002'
text = 'Depo Başlangıç Kiti ürününü stoklu alternatifle değiştir.'
expected = 'no_change'
options = {'no_recommendations': True, 'preludes': ('Depo Başlangıç Kiti 1 adet ekle.',)}

    @pytest.mark.parametrize("case,quote,text,expected,options", CASES, ids=[c[0] for c in CASES])
    async def test_reaudit4_variant(db, case, quote, text, expected, options):
>       await check_variant(db, quote, text, expected, **options)

tests/test_reaudit4_matrix.py:110: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa0fb99d0>
quote = 'Q-1002'
text = 'Depo Başlangıç Kiti ürününü stoklu alternatifle değiştir.'
expected = 'no_change', preludes = ('Depo Başlangıç Kiti 1 adet ekle.',)
topic = None, knowledge = None, no_recommendations = True, read = False
recommendations = None

    async def check_variant(
        db,
        quote,
        text,
        expected,
        preludes=(),
        *,
        topic=None,
        knowledge=None,
        no_recommendations=False,
        read=False,
        recommendations=None,
    ):
        app, client = await chat_client(db)
        async with app.router.lifespan_context(app), client:
            sid = await open_session(client, quote, CUSTOMERS[quote])
            for prelude in preludes:
                setup = await client.post("/api/chat", json=message(sid, prelude, quote))
                assert setup.status_code == 200, setup.text
            before = (await client.get(f"/api/quotes/{quote}")).json()
            async with db.connect() as conn:
                receipts_before = await conn.scalar(
                    sa.select(sa.func.count()).select_from(mutation_receipts)
                )
            request = message(sid, text, quote)
            response = await client.post("/api/chat", json=request)
            assert response.status_code == 200, response.text
            after = (await client.get(f"/api/quotes/{quote}")).json()
        async with db.connect() as conn:
            receipts_after = await conn.scalar(
                sa.select(sa.func.count()).select_from(mutation_receipts)
            )
            logs = (
                (
                    await conn.execute(
                        sa.select(tool_call_logs).where(
                            tool_call_logs.c.message_id == request["message_id"]
                        )
                    )
                )
                .mappings()
                .all()
            )
        body = response.json()
        attempted = [log["tool_name"] for log in logs if log["tool_name"] in MUTATIONS]
        applied = [log for log in logs if log["tool_name"] in MUTATIONS and log["mutation_applied"]]
        receipts = receipts_after - receipts_before
        unchanged = after == before and receipts == 0 and not applied
        items = {p["product_id"]: p["quantity"] for p in after["items"]}
        if expected == NO_CHANGE:
            assert unchanged, (items, body["notice"])
            # The planner itself refuses; a refusal must not depend on an executor guard.
            assert not attempted, (attempted, body["notice"])
            assert body["notice"] or topic or knowledge or read
        elif isinstance(expected, tuple):
            assert (unchanged and not attempted) or items == expected[1], (items, body["notice"])
        else:
            assert items == expected and not unchanged, (items, body["notice"])
        if not unchanged:
            # One receipt and one quote version per applied mutation, nothing else.
            assert applied and receipts == len(applied) == after["version"] - before["version"]
        if no_recommendations:
>           assert body["recommended_product_ids"] == [], body["recommended_product_ids"]
E           AssertionError: ['PRD-KIT-610', 'PRD-KIT-620']
E           assert ['PRD-KIT-610', 'PRD-KIT-620'] == []
E             
E             Left contains 2 more items, first extra item: 'PRD-KIT-610'
E             Use -v to get more diff

tests/test_reaudit_matrix.py:198: AssertionError
___________________ test_reaudit4_variant[s_update_mismatch] ___________________

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa0fe2e90>
case = 's_update_mismatch', quote = 'Q-1002'
text = 'BluePrint 80 miktarını 2 adede güncelle; 58 mm zorunlu.'
expected = 'no_change', options = {'preludes': ('BluePrint 80 1 adet ekle.',)}

    @pytest.mark.parametrize("case,quote,text,expected,options", CASES, ids=[c[0] for c in CASES])
    async def test_reaudit4_variant(db, case, quote, text, expected, options):
>       await check_variant(db, quote, text, expected, **options)

tests/test_reaudit4_matrix.py:110: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa0fe2e90>
quote = 'Q-1002'
text = 'BluePrint 80 miktarını 2 adede güncelle; 58 mm zorunlu.'
expected = 'no_change', preludes = ('BluePrint 80 1 adet ekle.',), topic = None
knowledge = None, no_recommendations = False, read = False
recommendations = None

    async def check_variant(
        db,
        quote,
        text,
        expected,
        preludes=(),
        *,
        topic=None,
        knowledge=None,
        no_recommendations=False,
        read=False,
        recommendations=None,
    ):
        app, client = await chat_client(db)
        async with app.router.lifespan_context(app), client:
            sid = await open_session(client, quote, CUSTOMERS[quote])
            for prelude in preludes:
                setup = await client.post("/api/chat", json=message(sid, prelude, quote))
                assert setup.status_code == 200, setup.text
            before = (await client.get(f"/api/quotes/{quote}")).json()
            async with db.connect() as conn:
                receipts_before = await conn.scalar(
                    sa.select(sa.func.count()).select_from(mutation_receipts)
                )
            request = message(sid, text, quote)
            response = await client.post("/api/chat", json=request)
>           assert response.status_code == 200, response.text
E           AssertionError: {"error":{"code":"REQUIRED_FEATURE_MISSING","detail":"Ürün gerekli özellikleri karşılamıyor."}}
E           assert 422 == 200
E            +  where 422 = <Response [422 Unprocessable Entity]>.status_code

tests/test_reaudit_matrix.py:162: AssertionError
____________________ test_reaudit4_variant[s_replace_match] ____________________

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa0f009d0>
case = 's_replace_match', quote = 'Q-1002'
text = 'BluePrint 80 ürününü 58mm stoklu alternatifle değiştir.'
expected = {'PRD-PRN-310': 1}
options = {'preludes': ('BluePrint 80 1 adet ekle.',)}

    @pytest.mark.parametrize("case,quote,text,expected,options", CASES, ids=[c[0] for c in CASES])
    async def test_reaudit4_variant(db, case, quote, text, expected, options):
>       await check_variant(db, quote, text, expected, **options)

tests/test_reaudit4_matrix.py:110: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa0f009d0>
quote = 'Q-1002'
text = 'BluePrint 80 ürününü 58mm stoklu alternatifle değiştir.'
expected = {'PRD-PRN-310': 1}, preludes = ('BluePrint 80 1 adet ekle.',)
topic = None, knowledge = None, no_recommendations = False, read = False
recommendations = None

    async def check_variant(
        db,
        quote,
        text,
        expected,
        preludes=(),
        *,
        topic=None,
        knowledge=None,
        no_recommendations=False,
        read=False,
        recommendations=None,
    ):
        app, client = await chat_client(db)
        async with app.router.lifespan_context(app), client:
            sid = await open_session(client, quote, CUSTOMERS[quote])
            for prelude in preludes:
                setup = await client.post("/api/chat", json=message(sid, prelude, quote))
                assert setup.status_code == 200, setup.text
            before = (await client.get(f"/api/quotes/{quote}")).json()
            async with db.connect() as conn:
                receipts_before = await conn.scalar(
                    sa.select(sa.func.count()).select_from(mutation_receipts)
                )
            request = message(sid, text, quote)
            response = await client.post("/api/chat", json=request)
            assert response.status_code == 200, response.text
            after = (await client.get(f"/api/quotes/{quote}")).json()
        async with db.connect() as conn:
            receipts_after = await conn.scalar(
                sa.select(sa.func.count()).select_from(mutation_receipts)
            )
            logs = (
                (
                    await conn.execute(
                        sa.select(tool_call_logs).where(
                            tool_call_logs.c.message_id == request["message_id"]
                        )
                    )
                )
                .mappings()
                .all()
            )
        body = response.json()
        attempted = [log["tool_name"] for log in logs if log["tool_name"] in MUTATIONS]
        applied = [log for log in logs if log["tool_name"] in MUTATIONS and log["mutation_applied"]]
        receipts = receipts_after - receipts_before
        unchanged = after == before and receipts == 0 and not applied
        items = {p["product_id"]: p["quantity"] for p in after["items"]}
        if expected == NO_CHANGE:
            assert unchanged, (items, body["notice"])
            # The planner itself refuses; a refusal must not depend on an executor guard.
            assert not attempted, (attempted, body["notice"])
            assert body["notice"] or topic or knowledge or read
        elif isinstance(expected, tuple):
            assert (unchanged and not attempted) or items == expected[1], (items, body["notice"])
        else:
>           assert items == expected and not unchanged, (items, body["notice"])
E           AssertionError: ({'PRD-PRN-320': 1}, 'Değiştirilecek kalemi tek anlamlı seçemedim. Ürün kodunu belirtir misin?')
E           assert ({'PRD-PRN-320': 1} == {'PRD-PRN-310': 1}
E             
E             Left contains 1 more item:
E             {'PRD-PRN-320': 1}
E             Right contains 1 more item:
E             {'PRD-PRN-310': 1}
E             Use -v to get more diff)

tests/test_reaudit_matrix.py:193: AssertionError
__________________ test_reaudit4_variant[s_source_size_named] __________________

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa13ac390>
case = 's_source_size_named', quote = 'Q-1002'
text = '80 mm BluePrint 80 ürününü RedPrint 58 ile değiştir.'
expected = {'PRD-PRN-310': 1}
options = {'preludes': ('BluePrint 80 1 adet ekle.',)}

    @pytest.mark.parametrize("case,quote,text,expected,options", CASES, ids=[c[0] for c in CASES])
    async def test_reaudit4_variant(db, case, quote, text, expected, options):
>       await check_variant(db, quote, text, expected, **options)

tests/test_reaudit4_matrix.py:110: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa13ac390>
quote = 'Q-1002', text = '80 mm BluePrint 80 ürününü RedPrint 58 ile değiştir.'
expected = {'PRD-PRN-310': 1}, preludes = ('BluePrint 80 1 adet ekle.',)
topic = None, knowledge = None, no_recommendations = False, read = False
recommendations = None

    async def check_variant(
        db,
        quote,
        text,
        expected,
        preludes=(),
        *,
        topic=None,
        knowledge=None,
        no_recommendations=False,
        read=False,
        recommendations=None,
    ):
        app, client = await chat_client(db)
        async with app.router.lifespan_context(app), client:
            sid = await open_session(client, quote, CUSTOMERS[quote])
            for prelude in preludes:
                setup = await client.post("/api/chat", json=message(sid, prelude, quote))
                assert setup.status_code == 200, setup.text
            before = (await client.get(f"/api/quotes/{quote}")).json()
            async with db.connect() as conn:
                receipts_before = await conn.scalar(
                    sa.select(sa.func.count()).select_from(mutation_receipts)
                )
            request = message(sid, text, quote)
            response = await client.post("/api/chat", json=request)
            assert response.status_code == 200, response.text
            after = (await client.get(f"/api/quotes/{quote}")).json()
        async with db.connect() as conn:
            receipts_after = await conn.scalar(
                sa.select(sa.func.count()).select_from(mutation_receipts)
            )
            logs = (
                (
                    await conn.execute(
                        sa.select(tool_call_logs).where(
                            tool_call_logs.c.message_id == request["message_id"]
                        )
                    )
                )
                .mappings()
                .all()
            )
        body = response.json()
        attempted = [log["tool_name"] for log in logs if log["tool_name"] in MUTATIONS]
        applied = [log for log in logs if log["tool_name"] in MUTATIONS and log["mutation_applied"]]
        receipts = receipts_after - receipts_before
        unchanged = after == before and receipts == 0 and not applied
        items = {p["product_id"]: p["quantity"] for p in after["items"]}
        if expected == NO_CHANGE:
            assert unchanged, (items, body["notice"])
            # The planner itself refuses; a refusal must not depend on an executor guard.
            assert not attempted, (attempted, body["notice"])
            assert body["notice"] or topic or knowledge or read
        elif isinstance(expected, tuple):
            assert (unchanged and not attempted) or items == expected[1], (items, body["notice"])
        else:
>           assert items == expected and not unchanged, (items, body["notice"])
E           AssertionError: ({'PRD-PRN-320': 1}, 'İstediğin hedef ürün fiyat, stok veya özellik koşullarını sağlamıyor. Teklifi değiştirmedim.')
E           assert ({'PRD-PRN-320': 1} == {'PRD-PRN-310': 1}
E             
E             Left contains 1 more item:
E             {'PRD-PRN-320': 1}
E             Right contains 1 more item:
E             {'PRD-PRN-310': 1}
E             Use -v to get more diff)

tests/test_reaudit_matrix.py:193: AssertionError
_______________ test_reaudit4_variant[s_source_size_target_size] _______________

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa1080a90>
case = 's_source_size_target_size', quote = 'Q-1002'
text = '80mm BluePrint 80 ürününü 58mm RedPrint 58 ile değiştir.'
expected = {'PRD-PRN-310': 1}
options = {'preludes': ('BluePrint 80 1 adet ekle.',)}

    @pytest.mark.parametrize("case,quote,text,expected,options", CASES, ids=[c[0] for c in CASES])
    async def test_reaudit4_variant(db, case, quote, text, expected, options):
>       await check_variant(db, quote, text, expected, **options)

tests/test_reaudit4_matrix.py:110: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa1080a90>
quote = 'Q-1002'
text = '80mm BluePrint 80 ürününü 58mm RedPrint 58 ile değiştir.'
expected = {'PRD-PRN-310': 1}, preludes = ('BluePrint 80 1 adet ekle.',)
topic = None, knowledge = None, no_recommendations = False, read = False
recommendations = None

    async def check_variant(
        db,
        quote,
        text,
        expected,
        preludes=(),
        *,
        topic=None,
        knowledge=None,
        no_recommendations=False,
        read=False,
        recommendations=None,
    ):
        app, client = await chat_client(db)
        async with app.router.lifespan_context(app), client:
            sid = await open_session(client, quote, CUSTOMERS[quote])
            for prelude in preludes:
                setup = await client.post("/api/chat", json=message(sid, prelude, quote))
                assert setup.status_code == 200, setup.text
            before = (await client.get(f"/api/quotes/{quote}")).json()
            async with db.connect() as conn:
                receipts_before = await conn.scalar(
                    sa.select(sa.func.count()).select_from(mutation_receipts)
                )
            request = message(sid, text, quote)
            response = await client.post("/api/chat", json=request)
            assert response.status_code == 200, response.text
            after = (await client.get(f"/api/quotes/{quote}")).json()
        async with db.connect() as conn:
            receipts_after = await conn.scalar(
                sa.select(sa.func.count()).select_from(mutation_receipts)
            )
            logs = (
                (
                    await conn.execute(
                        sa.select(tool_call_logs).where(
                            tool_call_logs.c.message_id == request["message_id"]
                        )
                    )
                )
                .mappings()
                .all()
            )
        body = response.json()
        attempted = [log["tool_name"] for log in logs if log["tool_name"] in MUTATIONS]
        applied = [log for log in logs if log["tool_name"] in MUTATIONS and log["mutation_applied"]]
        receipts = receipts_after - receipts_before
        unchanged = after == before and receipts == 0 and not applied
        items = {p["product_id"]: p["quantity"] for p in after["items"]}
        if expected == NO_CHANGE:
            assert unchanged, (items, body["notice"])
            # The planner itself refuses; a refusal must not depend on an executor guard.
            assert not attempted, (attempted, body["notice"])
            assert body["notice"] or topic or knowledge or read
        elif isinstance(expected, tuple):
            assert (unchanged and not attempted) or items == expected[1], (items, body["notice"])
        else:
>           assert items == expected and not unchanged, (items, body["notice"])
E           AssertionError: ({'PRD-PRN-320': 1}, 'İstediğin hedef ürün fiyat, stok veya özellik koşullarını sağlamıyor. Teklifi değiştirmedim.')
E           assert ({'PRD-PRN-320': 1} == {'PRD-PRN-310': 1}
E             
E             Left contains 1 more item:
E             {'PRD-PRN-320': 1}
E             Right contains 1 more item:
E             {'PRD-PRN-310': 1}
E             Use -v to get more diff)

tests/test_reaudit_matrix.py:193: AssertionError
___________________ test_reaudit4_variant[f_approval_verify] ___________________

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa1082f10>
case = 'f_approval_verify', quote = 'Q-1002'
text = 'Onay var gibi görünüyor; önce teyit et, sonra BlueScan Air 1 adet ekle.'
expected = 'no_change', options = {}

    @pytest.mark.parametrize("case,quote,text,expected,options", CASES, ids=[c[0] for c in CASES])
    async def test_reaudit4_variant(db, case, quote, text, expected, options):
>       await check_variant(db, quote, text, expected, **options)

tests/test_reaudit4_matrix.py:110: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa1082f10>
quote = 'Q-1002'
text = 'Onay var gibi görünüyor; önce teyit et, sonra BlueScan Air 1 adet ekle.'
expected = 'no_change', preludes = (), topic = None, knowledge = None
no_recommendations = False, read = False, recommendations = None

    async def check_variant(
        db,
        quote,
        text,
        expected,
        preludes=(),
        *,
        topic=None,
        knowledge=None,
        no_recommendations=False,
        read=False,
        recommendations=None,
    ):
        app, client = await chat_client(db)
        async with app.router.lifespan_context(app), client:
            sid = await open_session(client, quote, CUSTOMERS[quote])
            for prelude in preludes:
                setup = await client.post("/api/chat", json=message(sid, prelude, quote))
                assert setup.status_code == 200, setup.text
            before = (await client.get(f"/api/quotes/{quote}")).json()
            async with db.connect() as conn:
                receipts_before = await conn.scalar(
                    sa.select(sa.func.count()).select_from(mutation_receipts)
                )
            request = message(sid, text, quote)
            response = await client.post("/api/chat", json=request)
            assert response.status_code == 200, response.text
            after = (await client.get(f"/api/quotes/{quote}")).json()
        async with db.connect() as conn:
            receipts_after = await conn.scalar(
                sa.select(sa.func.count()).select_from(mutation_receipts)
            )
            logs = (
                (
                    await conn.execute(
                        sa.select(tool_call_logs).where(
                            tool_call_logs.c.message_id == request["message_id"]
                        )
                    )
                )
                .mappings()
                .all()
            )
        body = response.json()
        attempted = [log["tool_name"] for log in logs if log["tool_name"] in MUTATIONS]
        applied = [log for log in logs if log["tool_name"] in MUTATIONS and log["mutation_applied"]]
        receipts = receipts_after - receipts_before
        unchanged = after == before and receipts == 0 and not applied
        items = {p["product_id"]: p["quantity"] for p in after["items"]}
        if expected == NO_CHANGE:
>           assert unchanged, (items, body["notice"])
E           AssertionError: ({'PRD-BC-110': 1}, '')
E           assert False

tests/test_reaudit_matrix.py:186: AssertionError
____________________ test_reaudit4_variant[f_generic_id_qr] ____________________

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa1402350>
case = 'f_generic_id_qr', quote = 'Q-1001'
text = 'QR zorunlu; PRD-BC-110 ürününü stoklu alternatifle değiştir.'
expected = 'no_change', options = {'no_recommendations': True}

    @pytest.mark.parametrize("case,quote,text,expected,options", CASES, ids=[c[0] for c in CASES])
    async def test_reaudit4_variant(db, case, quote, text, expected, options):
>       await check_variant(db, quote, text, expected, **options)

tests/test_reaudit4_matrix.py:110: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

db = <sqlalchemy.ext.asyncio.engine.AsyncEngine object at 0xffffa1402350>
quote = 'Q-1001'
text = 'QR zorunlu; PRD-BC-110 ürününü stoklu alternatifle değiştir.'
expected = 'no_change', preludes = (), topic = None, knowledge = None
no_recommendations = True, read = False, recommendations = None

    async def check_variant(
        db,
        quote,
        text,
        expected,
        preludes=(),
        *,
        topic=None,
        knowledge=None,
        no_recommendations=False,
        read=False,
        recommendations=None,
    ):
        app, client = await chat_client(db)
        async with app.router.lifespan_context(app), client:
            sid = await open_session(client, quote, CUSTOMERS[quote])
            for prelude in preludes:
                setup = await client.post("/api/chat", json=message(sid, prelude, quote))
                assert setup.status_code == 200, setup.text
            before = (await client.get(f"/api/quotes/{quote}")).json()
            async with db.connect() as conn:
                receipts_before = await conn.scalar(
                    sa.select(sa.func.count()).select_from(mutation_receipts)
                )
            request = message(sid, text, quote)
            response = await client.post("/api/chat", json=request)
            assert response.status_code == 200, response.text
            after = (await client.get(f"/api/quotes/{quote}")).json()
        async with db.connect() as conn:
            receipts_after = await conn.scalar(
                sa.select(sa.func.count()).select_from(mutation_receipts)
            )
            logs = (
                (
                    await conn.execute(
                        sa.select(tool_call_logs).where(
                            tool_call_logs.c.message_id == request["message_id"]
                        )
                    )
                )
                .mappings()
                .all()
            )
        body = response.json()
        attempted = [log["tool_name"] for log in logs if log["tool_name"] in MUTATIONS]
        applied = [log for log in logs if log["tool_name"] in MUTATIONS and log["mutation_applied"]]
        receipts = receipts_after - receipts_before
        unchanged = after == before and receipts == 0 and not applied
        items = {p["product_id"]: p["quantity"] for p in after["items"]}
        if expected == NO_CHANGE:
            assert unchanged, (items, body["notice"])
            # The planner itself refuses; a refusal must not depend on an executor guard.
            assert not attempted, (attempted, body["notice"])
            assert body["notice"] or topic or knowledge or read
        elif isinstance(expected, tuple):
            assert (unchanged and not attempted) or items == expected[1], (items, body["notice"])
        else:
            assert items == expected and not unchanged, (items, body["notice"])
        if not unchanged:
            # One receipt and one quote version per applied mutation, nothing else.
            assert applied and receipts == len(applied) == after["version"] - before["version"]
        if no_recommendations:
>           assert body["recommended_product_ids"] == [], body["recommended_product_ids"]
E           AssertionError: ['PRD-BC-110']
E           assert ['PRD-BC-110'] == []
E             
E             Left contains one more item: 'PRD-BC-110'
E             Use -v to get more diff

tests/test_reaudit_matrix.py:198: AssertionError
=========================== short test summary info ============================
FAILED tests/test_reaudit4_matrix.py::test_reaudit4_variant[p_inflected_hundred_read]
FAILED tests/test_reaudit4_matrix.py::test_reaudit4_variant[p_inflected_hundred_write]
FAILED tests/test_reaudit4_matrix.py::test_reaudit4_variant[p_inflected_thousand_read]
FAILED tests/test_reaudit4_matrix.py::test_reaudit4_variant[p_inflected_thousand_write]
FAILED tests/test_reaudit4_matrix.py::test_reaudit4_variant[p_excess_payment_read]
FAILED tests/test_reaudit4_matrix.py::test_reaudit4_variant[p_business_days]
FAILED tests/test_reaudit4_matrix.py::test_reaudit4_variant[p_enfazla_quantity]
FAILED tests/test_reaudit4_matrix.py::test_reaudit4_variant[a_question_dogru]
FAILED tests/test_reaudit4_matrix.py::test_reaudit4_variant[a_question_oyle]
FAILED tests/test_reaudit4_matrix.py::test_reaudit4_variant[a_conditional_sanirsan]
FAILED tests/test_reaudit4_matrix.py::test_reaudit4_variant[a_pending_appearance]
FAILED tests/test_reaudit4_matrix.py::test_reaudit4_variant[a_settled] - Asse...
FAILED tests/test_reaudit4_matrix.py::test_reaudit4_variant[a_allowed] - Asse...
FAILED tests/test_reaudit4_matrix.py::test_reaudit4_variant[a_approved_adjective]
FAILED tests/test_reaudit4_matrix.py::test_reaudit4_variant[r_source_possessive]
FAILED tests/test_reaudit4_matrix.py::test_reaudit4_variant[r_unnamed_unbound_generic]
FAILED tests/test_reaudit4_matrix.py::test_reaudit4_variant[r_plus_free_generic]
FAILED tests/test_reaudit4_matrix.py::test_reaudit4_variant[r_no_substitutes]
FAILED tests/test_reaudit4_matrix.py::test_reaudit4_variant[s_update_mismatch]
FAILED tests/test_reaudit4_matrix.py::test_reaudit4_variant[s_replace_match]
FAILED tests/test_reaudit4_matrix.py::test_reaudit4_variant[s_source_size_named]
FAILED tests/test_reaudit4_matrix.py::test_reaudit4_variant[s_source_size_target_size]
FAILED tests/test_reaudit4_matrix.py::test_reaudit4_variant[f_approval_verify]
FAILED tests/test_reaudit4_matrix.py::test_reaudit4_variant[f_generic_id_qr]
24 failed, 67 passed in 18.09s

Exit code: 1
