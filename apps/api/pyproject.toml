[project]
name = "tbr-api"
version = "0.1.0"
description = "The Blue Red teklif asistanı API"
requires-python = ">=3.12,<3.13"
dependencies = [
    "fastapi==0.141.1",
    "uvicorn==0.53.0",
]

[tool.uv]
package = false
